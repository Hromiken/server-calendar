# 🗓️ Server Calendar

Простое HTTP API для управления календарными событиями.  
Поддерживает создание, обновление, удаление и выборку событий за день, неделю и месяц.  
Встроено **graceful shutdown** и Docker-сборка.

---

## 🚀 Запуск локально

```bash
   make run
